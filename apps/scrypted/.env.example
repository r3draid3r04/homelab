# Scrypted Environment Variables

# Watchtower API token for automatic updates
WATCHTOWER_HTTP_API_TOKEN=your_watchtower_token

# Enable periodic update polling (default: true)
WATCHTOWER_HTTP_API_PERIODIC_POLLS=true
